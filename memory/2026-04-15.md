# 2026-04-15

- Hermes 경유로 전달된 `shared-food-map.zip` 기반 작업을 이어받아, 워크스페이스 `shared-food-map/shared-food-map` 프로젝트의 지도 구현을 카카오맵에서 구글맵 기준으로 전환함.
- 구글맵 적용 시 필요한 클라이언트 환경변수는 `client/.env`의 `VITE_GOOGLE_MAPS_API_KEY`이며, Google Cloud Console에서 Maps JavaScript API 키를 발급받아 해당 변수값으로 넣는 방식으로 안내함.
- 개인 테스트 용도 기준으로는 Google Maps Platform 사용량이 보통 크게 튀지 않을 가능성이 높다고 설명했지만, billing 연결이 필요하고 0원 보장은 아니라는 점, API 키 공개/제한 미설정이 더 큰 리스크라는 점을 함께 정리함.
- 22:40 KST 마감 점검 크론 지시에 따라 당일 메모 생성, 장기 메모 최소 갱신, Git tracked 변경 커밋/`origin/master` 푸시까지 한 턴에 마감하는 운영을 다시 수행함.
