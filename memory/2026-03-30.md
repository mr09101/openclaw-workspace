## 2026-03-30
- 마스터는 소닉이 **Windows의 TradingView 데스크탑 앱 차트를 직접 보며 판단/트레이딩 보조**하길 원하며, 웹보다 데스크탑 앱 경로를 선호함.
- 이전 미완성 작업의 정체를 복구함: **WSL의 OpenClaw가 Windows의 TradingView/Chrome에 붙는 원격 CDP 브리지**를 만들다 중단된 상태였음.
- 오늘 진단한 핵심 blocker: `netsh interface portproxy`를 **9224 -> 9224**로 잡아 두면 TradingView 앱의 원래 **remote-debugging 포트(9224)** 를 포트프록시가 가로채서, WSL에서는 연결은 되더라도 `/json/version`에 **empty reply**가 발생함.
- 재발 방지 원칙: **앱이 쓰는 CDP 포트와 WSL용 portproxy 포트는 반드시 분리**한다. 권장 구조는 `TradingView 앱 = 9224`, `WSL/OpenClaw 진입용 프록시 = 9225`.
- 다음 재개 지점: 관리자 PowerShell에서 `9224` 포트프록시 삭제 → TradingView를 `--remote-debugging-port=9224`로 재실행 → `9225 -> 127.0.0.1:9224` 프록시 생성 → 이후 OpenClaw `browser.profiles.<name>.cdpUrl`를 WSL에서 도달 가능한 `http://<windows-host>:9225`로 연결.
- 개발 작업 운영 원칙을 `obra/superpowers` 스타일로 확정: **설계 → 계획 → 서브에이전트 실행 → 검증**을 기본 프레임으로 사용.
- 개발 요청은 메인 세션 단독 처리보다 `SONIC DEV TEAM` 역할 분업(소닉/테일즈/에이미/너클즈/실버/섀도우) 기반 서브에이전트 운영을 기본값으로 복구·유지하기로 재확인.
- 차트 분석 선호를 명확히 함: 마스터는 일반론보다 **차트 자체를 보고 구조·핵심 가격대·해석 이유까지 읽는 방식**을 원함.
- 커뮤니케이션 교훈: 사용자 질문이 남아 있을 때는 하트비트/실행완료/상태보고보다 **질문 직답을 첫 문장에 먼저** 해야 하며, 실제로 보고 있지 않은 TradingView를 보고 있다고 암시하면 안 됨.
- `external/blog-writer` 저장소 상태 점검 결과: 현재는 **클론만 되어 있고 설치/설정/실행 전 단계**( `.env`, venv, runtime, token, assets/input 부재, WSL 기준 `ffmpeg` 미설치 ).
- 마스터 요청으로 `gitlab.com/Starmixcraft/tesla-fsd-can-mod` 저장소 의미를 기억: Tesla CAN 버스의 오토파일럿 관련 메시지를 수정/재전송해 **FSD enable bit, 속도 프로필, nag suppression** 등을 제어하는 Adafruit Feather CAN 기반 펌웨어 프로젝트.
- webchat/control-ui에서 진행된 TradingView/YouTube 연결 작업 맥락을 텔레그램 메인 세션으로 동기화함. 이후 텔레그램에서도 동일한 연속 작업으로 취급.
- TradingView Desktop 연결 상태를 재확인한 동기화 메모 기준: Windows TradingView Desktop remote debugging + WSL 브리지 + OpenClaw `tvdesktop` 프로필 준비 완료, 실제 차트 탭(`https://kr.tradingview.com/chart/fgirMhSn/`)과 ETHUSDT.P/Binance/15분봉 화면 판독 성공.
- TradingView 해석 원칙을 재확인: 사용자 커스텀 지표/드로잉 의미는 추정하지 않고, **순수 price action(캔들, 최근 고점/저점, 수평 지지·저항, 돌파/이탈)** 만 사용.
- 모의매매는 기존 전략 엔진이 아니라 **TradingView 데스크탑 화면 기반 전용 paper trader**(`tv-vision-paper-trader`)로 운용하며, 파일 경로는 `trading-automation/scripts/capture_tradingview_desktop.js`, `trading-automation/runtime/tv_vision_paper_state.json`.
- YouTube는 브라우저 attach 방식으로 준비 완료: `ytbrowser` 프로필과 Windows 스크립트 `scripts/windows/start_youtube_browser_cdp.ps1`, `scripts/windows/expose_youtube_browser_cdp.ps1`를 사용해 화면 attach 가능.
- 운영 선호 재확인: webchat/control-ui에서 의미 있는 작업을 하면 텔레그램 메인 세션에도 핵심 컨텍스트를 동기화하는 쪽을 우선.
- 22:40 KST 하루 마감 점검 기준, 오늘의 장기 보존 핵심은 **TradingView Desktop 직접 판독 선호**, **Windows CDP ↔ WSL 브리지의 portproxy 포트 분리 원칙(앱 9224 / 프록시 9225)**, 그리고 **텔레그램 메인 세션까지의 맥락 동기화 완료**로 정리함.
