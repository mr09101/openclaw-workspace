## 2026-03-31
- TradingView 분석/모의거래 운영 선호를 구체화했다: **15분봉 기준 고정**, 화면이 15분봉이 아니면 먼저 전환, 알림은 **진입/청산·의미 있는 감시상태 변화** 위주로 줄이고, 그 외에는 **15분봉 마감마다 1줄 요약**만 보내는 방향으로 정리했다.
- 차트 감시는 고정 가격 알림 대기보다 **실시간 구조 변화에 맞춰 동적으로 해석을 업데이트**하는 방식으로 운용하기로 정리했다.
- TradingView Desktop 비전 paper trader 실행 흔적 기준, ETHUSDT.P 15분봉에서 **early short 1회 체결 후 target 청산(+1.1497 USDT)** 이 있었고, 이후에는 하락 구조 유지로 `watch_short_ready` 상태를 기록했다.
- Windows TradingView Desktop ↔ WSL/OpenClaw 연결은 계속 살아 있었고, `runtime/tv-current-check.json` 기준 **172.23.48.1:9225 CDP 브리지 + 실제 차트 페이지 연결** 상태를 다시 확인했다.
- `tradingview-indicators/tory-action-line` 쪽은 V2 방향을 문서화했다: 단순 최근 2개 pivot 연결 지표가 아니라 **선별된 action line + 구조 전환 후 arrow + 구조적 safety line** 중심의 상태형 지표로 가는 설계를 정리했다.
- 인사이트/지식 파이프라인은 오늘도 갱신되었다. `runtime/insights/state.json` 기준 누적 **383 runs / 192 collected / 15 applied / 5 behavior rules applied** 상태이며, knowledge index와 로컬 memory DB도 재생성됐다.
- 체크리스트성 자동화는 계속 돈 흔적이 남았다. 오전에는 `stock_bear_checklist`, 밤에는 `btc_bear_checklist` 관련 최신 리포트와 상태 파일이 갱신됐다.
- 22:40 KST 마감 점검에서는 당일 요약을 일일 메모로 새로 남기고, 장기 메모에는 TradingView 운영 선호 변경만 최소 반영한 뒤, 기록성 tracked 변경만 분리 커밋/푸시하는 원칙으로 닫는다.
