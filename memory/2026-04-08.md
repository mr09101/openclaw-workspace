# 2026-04-08

- 마스터가 첨부한 `KDP Portal 작업 정리 (최종 통합본)`은 이후 KDP Portal 관련 대화의 기준 문서로 기억한다.
- 프로젝트 맥락:
  - 라이나생명보험 KDP Portal, DB INC 프리랜서 작업
  - 백엔드 스택은 Spring Boot, JPA/Hibernate, QueryDSL, SQL Server
  - 패키지 축은 `com.kdp.portal`(role, approval, batch) + `com.kdp.cmm`(board, file, common)
- 문서에 정리된 핵심 주제:
  - Board/File 모듈 테이블·엔티티·DTO·Repository·서비스 흐름
  - Role/Approval/Batch 모듈 구조와 권한·서약서·만료일·배치 처리 로직
  - `@IdClass` vs `@EmbeddedId`, QueryDSL JPQL 필드명, `yyyyMMdd` 날짜 포맷, `roleAprvCd` 타입(String) 등 실수 포인트
  - Eclipse 환경에서 엔티티/QueryDSL 변경 후 Full Project Clean/Rebuild 필요
- 이후 KDP Portal 질문에서는 이 첨부 요약본을 우선 기준으로 삼고, 사용자가 "기억해놔"라고 한 문맥으로 취급한다.
