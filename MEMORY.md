# MEMORY

## 운영 원칙 (사용자 선호)
- 마스터는 반복 운영성 작업(예: 하루 마감 점검)을 소닉이 먼저 수행하고 결과만 보고하길 선호함.
- 마스터와의 1:1 대화에서는 항상 존댓말을 사용하고, 호칭은 "마스터"를 유지함.
- OpenClaw 소닉 세션에서는 작업 시작 첫머리에 `팀소닉 사용` / `팀소닉 미사용` 여부를 훅처럼 먼저 밝힌다. `TeamDark` 표기는 Hermes 문맥 전용으로 취급하고, 소닉 세션에서는 쓰지 않는다.
- 채널 설정은 **텔레그램↔현재 대화채널 양방향 동기화**를 기본으로 운용함. 어느 채널에서든 설정을 바꾸면 같은 턴에 공통 설정으로 반영한다.
- webchat/control-ui에서 의미 있는 작업(연결, 자동화, 전략 변경, 운영 상태 변경)을 수행하면 **텔레그램 메인 세션에도 핵심 컨텍스트를 반드시 동기화**해, 주 채널인 텔레그램이 같은 연속 작업으로 이어받도록 운용한다.
- Hermes를 메인 입구로 두고 OpenClaw로 자동 전달하는 브리지/릴레이 작업에서는, 자동 전달 관련 진행상황과 완료 보고를 **텔레그램 메인 세션에도 수시로 보내는 것**을 기본 운영으로 유지한다.
- 채널별 설정 기준은 텔레그램 운영값을 기본 템플릿으로 삼되, Web/Control UI에서 변경된 항목도 텔레그램 기준값에 즉시 병합한다(단방향 아님).
- 리서치/검색 기본 체인은 텔레그램 기준으로 고정: `hybrid_scrapling_search.py` 우선 → 본문추출(scrapling) → 실패 시 `web_fetch`/RSS 폴백.
- 동기화 절차: (1) 변경사항 확인 → (2) `MEMORY.md`/`USER.md` 갱신 → (3) 다음 응답부터 전 채널 동일 적용.
- 리마인더 문구를 전달할 때 사용자에게 다시 일을 시키는 표현을 피하고, 선제 실행 + 완료 보고를 우선함.
- 로또 번호 생성처럼 즉답 가능한 생성 요청은 스킬/내부구현 설명보다, 소닉이 대화에서 바로 번호와 필터 요약을 답하는 방식을 선호함.
- 일일 마감 점검(22:40 KST)은 "실행 먼저, 결과만 짧게 보고" 포맷을 기본값으로 유지.
- 마감 점검에는 메모 갱신뿐 아니라 Git 변경 정리(추적/커밋/푸시)까지 한 번에 끝내는 운영을 선호함.
- 마감 점검의 Git 처리 기준은 `tracked 변경(수정/삭제)` 우선이며, 해당 변경이 있을 때만 commit/push를 수행함.
- 마감 점검에서 untracked만 있을 경우에는 무리하게 add/commit하지 않고, commit/push 미수행으로 보고만 남기는 방식을 유지함.
- 마감 점검 보고는 사용자 액션 요청 없이, 실행 결과(메모 갱신/커밋·푸시 여부)만 짧게 전달하는 형식을 유지함.
- 크론형 리마인더에서도 사용자에게 추가 지시 없이, 가능한 작업은 선제 실행 후 결과만 짧게 보고하는 방식을 유지함.
- 크론형 마감 점검 지시(메모 갱신+Git 정리)가 오면, 사용자 확인을 기다리지 않고 같은 턴에 실행 완료(기록/커밋/푸시)까지 마무리하는 운영을 기본값으로 유지함.
- 마감 점검 지시가 체크리스트 형태(일일 메모·장기 메모·git push·짧은 결과보고)여도 동일하게 즉시 전항목 수행 후 결과만 보고하는 형식을 유지함.
- 하루 마감 점검은 야간/새벽 실행이라도 동일하게 즉시 수행하고, 보고는 실행 결과 요약만 전달하는 형식을 유지함.
- 마감 점검 크론에 "사용자에게 작업 지시/요청 금지" 조건이 포함되면, 추가 요청 없이 내부 실행 완료 후 결과만 단문 보고하는 방식을 유지함.
- 2026-04-29 재확인: 하루 마감 점검 크론은 같은 턴에서 일일 메모·장기 메모 최소 갱신과 tracked 변경 commit/push까지 닫고, 사용자에게는 결과 요약만 짧게 보고한다.
- 마감 점검 시 Git은 `tracked 변경` 기준으로만 커밋/푸시를 판단하고, `untracked-only` 상태에서는 메모 갱신 후 결과 보고로 마감하는 원칙을 유지함.
- 마감 점검 체크리스트(일일 메모/장기 메모/git push/짧은 결과보고)는 크론 이벤트 수신 턴에서 한 번에 끝내고, 사용자에게는 결과만 전달하는 운영을 유지함.
- 일일 마감 점검 시 해당 날짜 `memory/YYYY-MM-DD.md`가 없으면 즉시 생성해 핵심 요약을 남기는 원칙을 유지함.
- NotebookLM/NLM 인증 안내에서는 `폰만으로 완전 인증` 가능하다고 단정하지 않고, 필요 시 `nlm login --wsl`로 Windows Chrome을 열어 폰 승인까지 연결하는 경로를 우선 안내함.
- 외부 도구/서비스를 제거하거나 비활성화할 때는 국적·성격·신뢰성 같은 속성을 추정으로 단정하지 않고, 먼저 사실관계를 확인한 뒤 반영한다.
- 2026-04-09 기준 `btc-7step-checklist-alert`, `stock-7step-checklist-alert` 두 체크리스트 자동 알림 크론은 사용자 요청으로 중지(disabled) 상태를 유지한다.
- 향후 브리핑에서는 티이엠씨(425040) 종목을 기본 제외 대상으로 유지.
- Gmail/Calendar 관련 구글 연동 점검(gog 기반)은 별도 재지시 전까지 중지 상태를 유지.
- HEARTBEAT 주기 점검 기본셋은 `tasks/git/system`만 유지하고, email/calendar 점검은 비활성 정책으로 운용.
- Mission Control/대시보드 접속 포트는 고정값(예: 3005)으로 가정하지 않고, 항상 `openclaw status`로 현재 주소를 확인한 뒤 접속한다.
- Control UI의 `origin not allowed`는 브라우저 문제가 아니라 허용 origin 설정 문제로 우선 진단한다.

## 알림 문구 고정(그룹)
- 양배추 알람 문구(고정): `@hakitee “귀엽고 이쁜 하연님 양배추즙 먹을 시간이에요 💚”`

## OpenClaw 운영 핵심
- CLI에서 채팅 UI가 필요할 때 기본 진입 명령은 `openclaw tui`.
- 설정 꼬임/모델 연동 이슈 시에는 백업 설정 복원 후 `openclaw gateway restart`가 표준 복구 루트.
- X(트위터) 링크 조회는 `api.fxtwitter.com` 경유 방식이 실사용 가능하며, 본문/메타/이미지 확인까지 가능함.
- 텔레그램 체감형 스트리밍 이슈 점검 시 `draft 경로 설정`만 보지 말고 `sendMessage 400(message is too long)` 로그를 함께 확인해야 하며, 장문 실패 재발 시 `textChunkLimit`/`chunkMode` 튜닝을 우선 검토함.
- 텔레그램 direct 메인 세션은 컨텍스트가 약 90%대까지 비대해지면 `embedded run timeout`, `timed out during compaction`, `prompt-error: aborted`가 연쇄 발생할 수 있으므로, 채널 장애로 단정하지 말고 **해당 DM 세션 과포화**를 먼저 의심한다.
- `runtime/insights/state.json` 기준 인사이트 자동반영 크론이 예정 주기(매시 17분 KST) 대비 멈춰 있으면, 단순 보고 누락이 아니라 **수집→반영→개선효과 보고 루프 전체 중단**으로 간주하고 blocker로 취급한다.
- bundle MCP 서버(특히 `gbrain`) 기동 타임아웃이 반복될 때는, 무작정 gateway만 여러 번 재시작하지 말고 **해당 MCP 엔트리를 먼저 비활성화/분리한 last-good 설정으로 복구**하는 쪽을 우선 고려한다.
- cron 세션에서 `EXEC_BLOCKED`가 보이면 gateway 재시작보다 먼저 에이전트 tool allowlist 누락 여부(`exec/read/write`)를 우선 점검한다.
- `cron status`에서 jobs=0이면 `~/.openclaw/cron/jobs.json.bak` 복원 가능성을 우선 확인하고, 복원 후 재기동으로 신속 복구한다.
- `cron list`의 `error`는 "마지막 실행 결과"가 남아 보이는 상태일 수 있으므로, 일간 잡은 `cron runs --id <job>`로 최신 실패 원인(server_error/timeout 등)을 함께 확인해 실제 막힘 여부를 판단한다.
- OpenClaw 기본 모델 변경은 외부 카탈로그/Hermes 지원 여부만 믿고 적용하지 말고, 반드시 해당 런타임에서 `허용 모델 확인 → 1회성 실제 호출 테스트 → 성공 확인 후 기본값 변경` 순서로 검증한다.
- Hermes의 `Response truncated due to output length limit` 경고는 텔레그램 전송 실패보다 Hermes 내부 출력 길이 제한일 가능성을 우선 본다. X 스냅샷/FxTwitter JSON 같은 과다 입력은 핵심만 추려 짧게 답하는 방향을 기본으로 유지한다.
- OpenClaw 장애 복구 시 `gbrain` 같은 bundle MCP 서버가 반복 타임아웃을 내면, gateway 재기동만 반복하지 말고 해당 MCP 엔트리를 last-good 설정에서 먼저 비활성화/분리하는 복구 루트를 우선 고려한다.
- `openclaw gateway status`는 정상이지만 `openclaw status`만 timeout/SIGKILL로 재현될 때는 서비스 전체 장애보다 메인 DM 세션 과포화·상태조회 경로 과부하를 먼저 의심한다.

## 작업 방식 선호(신규)
- 마스터는 단일 스킬과 그래프형(쪼개기) 스킬을 상황별로 섞는 하이브리드 운영을 선호함.
- 기본은 빠른 단건 처리, 반복/심화/저장 요청 시 파이프라인형(PRO)으로 승격하는 방식을 채택.
- 학습 자료(PDF/문서) 처리 시 중복은 자동으로 걸러내고, 신규 내용만 요약·학습 반영하는 운영을 선호함.
- 트레이딩/차트 브리핑 계열에서는 수익 극대화보다 손실관리(사전 손절 기준 설계·기계적 실행)를 우선 프레임으로 유지.
- 데일리 브리핑 정보수집은 무료 소스 우선 원칙 적용: DuckDuckGo(키워드), CoinGecko(코인 지표), Google News/주요 매체 RSS.
- 데일리 브리핑 말미에 "오늘 장중 5줄 실행 템플릿"(장세판별/종목선정/진입트리거/손절·익절/마감규칙) 자동 첨부를 기본으로 유지.
- 데일리 브리핑의 섹터는 고정 리스트를 쓰지 않고, 당일 강한/유망 섹터를 유동적으로 선별해 폭넓게 보고.
- 데일리 브리핑 개수 규칙은 유연 운용: 미장/국장 각각 최소 5섹터, 좋은 섹터가 더 많으면 확장 보고. 섹터당 종목도 기본 3개이나 강한 후보가 있으면 3개 초과 허용.
- 자동매매 로드맵은 국장+코인 병행을 선호하며, 모의(종목/현물) 검증 후 실전 전환, 코인은 이후 선물로 확장하는 단계적 접근을 선호.
- 자동매매 논의는 보류 중일 때도 주기 리마인더로 상기받길 원하며, 사용자가 중지 지시할 때까지 유지하는 운영을 선호.
- 자동매매 리마인더 크론(`ff77adeb-d180-4a09-8c9d-24c8324a0c8b`)은 매일 10:00/18:00 KST로 운용 중.
- OpenClaw 자동 업데이트 점검/적용 크론(`openclaw-morning-auto-update`)은 매일 09:05/21:05 KST(하루 2회)로 운용.
- 2026-03-13 점검에서 2026.3.12 업데이트는 기동 크래시 회귀 이슈(깃허브 이슈 #44781, #45006) 근거로 보류하고 2026.3.11 유지 결정을 확정함.
- 2026-03-14 재점검(하루 5회)에서도 2026.3.12/2026.3.13의 OOM·기동/메모리 회귀 이슈 근거가 추가 확인되어, 안정성 우선으로 2026.3.11 유지 보류 결정을 재확인함.
- 마스터는 별도 지시 없이도 더 우수한 전략이 발견되면 paper 모드에서 자동 재평가/교체 수행을 선호함(단, live 주문은 잠금 유지).
- 마스터는 paper 운용 중 자동 브리핑(정기 상태 보고)과 진입/청산 발생 시 즉시 알림을 선호함.
- 진입/청산 알림의 `사유` 표기는 한국어 설명을 기본으로 선호함.
- 진입/청산 알림에는 `진입금액`과 `청산금액`을 콤마 포함 가독성 높은 형식으로 함께 표기하는 것을 선호함.
- paper 자동브리핑에는 성과 검증용으로 승률/실현·미실현 PnL/총손익/손익비(R:R)/PF를 함께 표기하는 것을 선호함.
- 투자/모의거래 보고에서는 현재 손익만이 아니라 누적 수익률·누적 손익도 함께 보이길 선호함.
- X 원글/스레드 해석에서는 본문만 보고 끝내지 말고, 항상 작성자 본인 답글·셀프리플라이까지 확인해 연속 스레드 여부를 먼저 검증한 뒤 설명하는 방식을 선호함.
- 영어권 트레이딩 영상/자료는 원문을 그대로 던지기보다, 자막·전사·용어정리·학습루틴 형태로 소화 가능한 공부 경로로 바꿔 안내하는 방식을 선호함.
- 브리핑 가독성은 표(테이블)형 출력을 선호하며, 핵심 지표가 한눈에 들어오게 구성하는 것을 선호함.
- 사용자 요청으로 증권사 리포트 컨센서스 요약 봇 구축/자동발송을 선호함(종목/섹터 프리셋 필터 포함).
- 증권사 리포트 자동체크의 최신 기준은 **국장+미장 동시 후보 정리 + 보기 좋은 포맷 + 종목별 핵심 호재 1줄 요약**이며, 예전 단순 장전 요약 크론보다 이 최신 포맷을 우선 기준으로 기억/운용해야 한다.
- 최신 리포트 자동화의 실전형 권장 스케줄은 **매일 08:40 KST + 21:30 KST** 2회 보고다(오전은 국장 시작 전 + 밤사이 미장 반영, 야간은 당일 국장 반영 + 미장 시작 전 점검).
- 마스터는 이 최신 리포트(국장+미장 후보 + 호재 요약)를 **한투 KIS 모의거래의 후보 선정 입력**으로 실제 연결하길 원하며, 기본 운용안은 `국장 TOP 후보 상위 3개 / 종목당 약 100만원 / 손절 -5% / 익절 +12% / 리포트 후보에서 사라질 때까지 보유 / 후보 이탈 시 교체` 기준으로 시작한다.
- 리포트기준 한투모의 상태 보고는 장중 **10:50 / 12:50 / 14:50 KST** 3회 주기 보고를 기본으로 유지하고, **장마감 상태 보고는 15:25 KST**에 1회 추가한다. 진입/청산/교체 같은 액션 발생 시에는 즉시 추가 보고한다.
- 2026-05-05 확정: 한투 KIS 모의거래(리포트기준 자동매매/상태보고 포함)는 **KRX 휴장일에는 실행·보고하지 않고 `NO_REPLY`로 스킵**하는 운영을 기본값으로 유지한다.
- 2026-05-05 확정: KRX 휴장일 판정은 외부 OTP 다운로드/라이브러리 의존보다, 워크스페이스의 `krx_market_calendar.py` 같은 로컬 자체 캘린더 기준을 우선 유지한다.
- 2026-04-21 확정: 최신 증권사 리포트 자동화(국장+미장 후보 + 종목별 핵심 호재 1줄)는 **한투 KIS 모의거래의 실제 후보 선정 입력으로 이미 연결된 운영 상태**로 기억한다. 현재 시작 기준은 `국장 리포트 후보 상위 3개 / 언급수 2건 이상 / 매수비율 80% 이상 / 상승여력 15% 이상 / 종목당 약 100만원 / 손절 -5% / 익절 +12% / 리포트 후보에서 사라질 때까지 보유 / 후보 이탈 시 교체`다.
- 08:50 장전 리포트 컨센서스 요약에는 종목별 `현재가`를 함께 표기하는 포맷을 선호함.
- 국장 리포트/후보 정리에는 **연기금 최근 순매수·순매도 추적**도 함께 붙여, 어떤 종목을 사고파는지 바로 보이게 하는 포맷을 선호함.
- `smalltalk-vending` 모바일 버전은 네온 키오스크보다 **아날로그 자판기 감성**을 선호하며, 내부 Wi-Fi 전제 대신 **외부 네트워크에서도 바로 열 수 있는 접근 경로**를 기본 요구사항으로 본다.
- arXiv 신규 스크리닝 알림은 매 배치마다 "우리 적용 가능성(적용/관찰/보류)" 코멘트와 적용 후보 요약을 함께 제공하는 것을 선호함.
- 마스터는 가능한 작업은 지시 없이 선제 실행을 선호하며, 애매하거나 리스크/외부행동이 수반되는 경우에는 먼저 실행 가능 여부를 짧게 확인받는 방식을 선호함.
- 자동화 누락 발생 시 변명보다 원인 분석+즉시 자가복구(감시/재실행 가드 추가) 방식의 개선을 선호함.
- 자동매매 paper 현행 기준 전략은 `BRK_lb30_s180_t350`이며, 6시간 20분 주기 자동 재평가/교체 크론(`aee0e94c-13c7-4e0a-ae6f-b409ead6c25e`)으로 관리하고 live 주문 잠금은 유지함.
- 현행 breakout 진입은 보완안 `30봉 고점 +0.2% 버퍼`를 적용해 가짜 돌파를 줄이는 방향으로 운용함(`entry.breakout_buffer_pct=0.2`).
- 마스터는 단기 성과 부진 구간에서는 즉시 리셋보다 "관찰 유지"를 우선하고, 부진이 지속되면 소닉이 외부 리서치(웹/문헌)까지 포함해 대안 전략을 주도적으로 설계·검증하길 선호함.
- 전략 자동 교체 알림 시에는 단순 교체 사실뿐 아니라 `무엇이 어떻게 바뀌었는지`(전략ID, lookback, stop_loss, take_profit from/to)를 함께 설명하는 보고를 선호함.
- 전략 관련 의견 제시 시 말로만 끝내지 말고, 가능하면 즉시 보완안 shadow 테스트를 같이 돌려 근거 수치(PF/Net/stress)를 제시하는 방식을 선호함.
- 마스터는 "최선 전략 탐색"을 상시 과제로 두고, 소닉이 주도적으로 외부 리서치(인터넷/논문)→가설 생성→백테스트→paper 반영 루프를 지속 운용하길 원함.
- 마스터는 별도 재지시 없이도 X/웹 리서치 + 차트/패턴 아이디어 접목 + 백테스트 + 기준 통과 시 모의(paper) 반영/검증을 상시 자동 수행하길 원함.
- 상시 운영 신뢰를 위해 정기 "증빙 리포트"(현재 전략, 마지막 보완 테스트 시각, 마지막 재평가 시각, 마지막 전략교체 시각, 24h 리포트 건수)를 자동 발송하는 방식을 선호함.
- "말만 하고 실행 안 함" 방지를 위해 활동 정체(예: 10시간 무활동) 자동 경고 가드를 함께 운용하는 것을 선호함.
- X 아티클(ACP 연동/운영 꼬임 사례) 반영으로 에이전트 운영 무결성 점검(워크스페이스·paper lock·runner·ACP 실행경로·git remote) 자동 체크를 6시간 주기로 운용함 (`f5cec661-5d8e-46b2-907a-a1156fe806c4`).
- 리서치 지속학습을 위해 md/json 기반 Knowledge Index 루프를 도입함(`runtime/knowledge_index.json`, 4시간 주기 리빌드 크론 `fc3c031b-7634-4c71-be88-7bbd8a49adf3`).
- arXiv의 AI/금융 카테고리(cs.AI, q-fin.ST, q-fin.TR) 신규 논문 자동 스크리닝을 6시간 주기로 운용함(`cbc6eb5a-2a7b-4fc0-87e4-f5f976e06e3c`).
- 마스터 지시로 arXiv 스크리닝 배치마다 전략 검증/자동적용 루프를 함께 수행하도록 고정함(매 배치 `strategy_full_sweep_backtest.py` + `strategy_auto_upgrade.py` 실행, 변경 시 즉시 보고).
- 마스터는 arXiv에 한정하지 않고, 다른 루트(웹/X/브리핑/리포트/내부 후보)에서 좋은 아이디어가 포착될 때마다 동일하게 "검증→적용" 무한발전 루프를 상시 실행하길 원함.
- 마스터는 "검증이 실제로 수행됐는지" 확인 가능하도록, 전략 검증/재평가 배치가 돌 때마다 결과를 매회 보고받는 방식을 선호함(미변경이어도 요약 보고).
- 마스터는 전략 관련 모든 보고(검증/재평가/브리핑/증빙)에 `전략설명` 1줄을 포함해, 현재 전략이 무엇을 하는지 평문으로 확인 가능하길 선호함.
- 텔레그램 모바일(갤럭시 S23+) 가독성을 위해 투자 브리핑 표는 가로형 다열표 대신 `2열 세로형 표`(짧은 셀, 카드형 분할)를 선호함.
- paper 자동브리핑(매시간) 코인 요약표에는 `USDT 가격` 항목을 항상 함께 표기하길 선호함.
- 체크리스트류 출력(제목/항목명/상태/사유)은 전부 한국어 표기를 기본값으로 유지하길 선호함.
- 차트 분석 요청 시에는 일반론보다 **차트 자체를 보고** 구조·핵심 가격대·왜 그런 해석이 나왔는지까지 읽어내는 방식의 답변을 선호함.
- TradingView에 표시된 사용자 커스텀 지표/초대받아 켜진 인디케이터는 해석 대상에서 제외하고, **순수 가격차트(price action)만 기준**으로 판단하길 원함.
- TradingView 기반 모의거래는 **15분봉 기준**으로 판단하되, 현재 화면이 15분봉이 아니면 먼저 TradingView를 15분봉으로 전환한 뒤 판단하길 원함.
- TradingView 기반 모의거래 알림은 너무 잦지 않게, **진입/청산 또는 특별한 감시 이벤트(의미 있는 감시상태 생성/변화/해제)** 가 있을 때만 보내길 원함.
- 위 특별이벤트가 없더라도, TradingView 기반 모의거래는 **15분봉 마감마다 1줄 요약**은 받길 원함.
- 차트 감시는 고정 임계값만 기다리는 식보다, **계속 차트를 보면서 구조 변화에 맞춰 동적으로 생각을 업데이트**하는 방식으로 운용하길 원함.
- 앞으로 개발 작업에서는 `obra/superpowers`식 워크플로(설계→계획→서브에이전트 실행→검증)를 가능한 기본 프레임으로 활용하길 선호함.
- 개발 요청은 가능하면 메인 세션 단독 처리보다 `SONIC DEV TEAM` 역할 분업(소닉/테일즈/에이미/너클즈/실버/섀도우) 기반 서브에이전트 운영을 기본값으로 복구·유지하길 원함.
- 장시간 `running` 상태로 멈춘 서브에이전트는 정상 장기 실행으로 낙관하지 말고 blocker 후보로 분류해 별도 점검 대상으로 올린다.
- 소닉팀 구조를 이미 깔아둔 개발 작업에서는 이름 없는 generic subagent를 임의로 쓰지 않는다. 먼저 `sonic-pm`, `tails-backend`, `amy-frontend`, `knux-devops`, `silver-qa`, `shadow-reviewer` 역할 에이전트로 라우팅하고, 예외가 필요하면 사유를 먼저 설명하거나 마스터 허용을 받은 뒤에만 generic subagent를 사용한다.
- 브레이크아웃 전략의 과열 추격(고점 돌파 직후 진입) 성향을 낮추는 보완을 선호함.
- 롱 전략 손절이 반복될 때는 진입 타점 역방향(숏 미러) 가설을 paper shadow로 1~3일 검증해 근거 수치(PF/순손익)로 판단받는 방식을 선호함.
- 숏 미러 shadow 검증은 사용자 중지 지시 전까지 지속 운용(기본 1시간 주기)하는 것을 선호함.
- 마스터는 친구와 진행 중인 시니어 커뮤니티 프로젝트 "노다지"의 진행상황(결정/산출물/다음 단계)을 지속적으로 저장·추적하길 원함.
- 마스터는 외부 인사이트를 수집하면 소닉이 자동으로 반영하고, 반영 후 "무엇을 반영했고 무엇이 개선됐는지"를 결과 중심으로 보고하길 원함.
- 마스터는 인사이트 파이프라인에서 수집까지 포함한 완전 자동화를 원함(수집→반영→개선효과 보고 전과정 자동).
- 마스터는 인사이트 자동화 보고 시 "무엇을 수집했고, 왜 반영/미반영됐는지"를 항목별로 명확히 포함하길 원함(사유 없는 요약 보고 불만).
- 마스터는 업그레이드/반영 보고 때마다 `반영 항목`과 `미반영 항목`을 각각 점수·사유와 함께 분리해 설명받길 원함.
- 마스터는 업그레이드 보고에서 제목만이 아니라 각 항목의 `무슨 내용인지 요약`까지 함께 받길 원함.
- 마스터는 인사이트 반영 보고에서 `무엇을 파일에 적재했는지`보다 `실제 행동이 무엇이 바뀌었는지`를 우선해서 명확히 설명받길 원함.
- 마스터는 같은 내용의 중복 응답을 매우 싫어하며, 변경점 없는 반복 보고는 금지(1회 간결 보고 원칙).
- 마스터는 브리핑/운영 보고에서 결과뿐 아니라 "왜 거래가 없었는지" 같은 원인 설명을 함께 원함(형식적 장문 요약보다 실행원인 중심 선호).
- 마스터는 확인하지 않은 설정·수치·상태를 추정으로 단정해 말하는 것을 매우 싫어하며, 불명확하면 먼저 확인하거나 모른다고 인정한 뒤 검증 계획을 짧게 제시하는 방식을 선호함.
- 자동매매/전략 설명에서는 `stock_paper` 같은 전역 엔진 설정과 특정 실행기(예: KIS 리포트 전용 전략)의 개별 설정을 섞어 말하지 않고, 반드시 **어느 파일/전략 단위의 값인지 분리 확인 후 설명**하는 원칙을 유지한다.
- 종목 발굴/후보 카드의 리스크 표시는 영어 내부코드 대신 **한국어 문구**를 기본값으로 유지하길 선호하며, 공매도/숏리포트 체크는 후보 승격 전 **리스크 게이트**로 반영하는 방향을 선호함.
- 마스터는 코인뿐 아니라 한투(KIS) 모의투자도 브리핑 템플릿과 연결해 실제 자동 실행되길 강하게 원함.
- 마스터는 코인/주식 모의를 동시에 돌리고, 주식도 코인처럼 진입·보유·손익 상태를 표로 정기 보고받길 원함.

## 신뢰/커뮤니케이션 원칙(강화)
- 검증되지 않은 기능을 "된다"고 단정하지 않는다.
- 설정값·보유수량·전략 파라미터·운영 상태는 추정으로 말하지 않고, 파일/로그/실행 결과로 먼저 확인한 뒤 답한다.
- 사용자 요청 작업은 시작 전에 가능/불가 범위를 먼저 명확히 설명한다.
- 불확실하거나 제한이 있으면 즉시 인정하고, 대체 가능한 실행안만 제시한다.
- 사용자가 "이미 설정/적용했다"고 말한 항목은 반박하지 말고, 먼저 설정/로그 증빙을 확인한 뒤 답한다.
- 특정 툴 실패를 이유로 멈추지 않고 즉시 우회 경로(`web_fetch`/browser/대체 소스)로 실행을 이어간다.
- Git 변경 작업 시 `force push`/브랜치 삭제/히스토리 재작성은 명시 지시+확인 없이는 수행하지 않는다.
- 설정 변경은 추측으로 진행하지 않고, 문서 확인 후 백업/복구 경로 확보를 선행한다.
- OpenClaw 업데이트/복구/설정 변경에서는 `openclaw doctor --fix`, sandbox 관련 키, `tools.fs.workspaceOnly` 같은 민감 설정을 사용자 명시 요청이나 장애 복구 근거 없이 임의 적용하지 않는다. 적용 전에는 반드시 현재 설정 백업, 변경 diff 확인, 적용 후 `workspace writable / MEMORY.md·memory·.git 존재 / 필요한 호스트 연결` 헬스체크를 수행하고, 이상 시 즉시 이전 설정으로 롤백한다.
- 사용자가 방금 물은 질문에 대한 **직접 답변을 첫 문장에 먼저** 준다. 관련 없는 상태보고/하트비트/부가설명은 그 뒤로 미루거나 생략한다.
- 시스템 이벤트/하트비트/실행완료 메시지가 끼어들어도, 사용자의 미답변 질문보다 우선 처리하지 않는다. 사용자 질문이 남아 있으면 그 질문에 먼저 답한다.
- 같은 턴에 중복 답변(태그 버전 + 일반 버전 반복)을 보내지 않는다. 사용자에게 보이는 최종 답변은 한 번만 보낸다.
- 질문 의도가 애매하면 추측해서 옆길로 답하지 말고, 1문장으로 정확히 무엇을 이어갈지 확인한다.
- 사용자가 번호만 언급하는 후속 질문을 하면, 먼저 같은 대화의 바로 직전 목록/요약/최근 메시지를 읽어 기준 대상을 추론한 뒤 답하고, 그래도 불충분할 때만 짧게 되묻는다.
- GitHub 상위 repo/외부 오픈소스를 운영에 반영할 때는 GitHub를 설치·채굴·검증 출처로만 쓰고, 실제 운영은 가능한 한 로컬 문서/스크립트/스킬로 이식해 원본 repo 업데이트에 종속되지 않는 방식(로컬화/cherry-pick)을 기본으로 유지한다.
- 외부 워크플로/도구를 도입할 때는 upstream 실행 경로(`uvx`/PyPI 직결, 원격 repo 의존)보다 **로컬 clone + 로컬 venv/스크립트 + OpenClaw 등록** 형태로 고정해, 업데이트나 로그인 상태 변화에 덜 흔들리는 로컬 우선 구조를 선호한다.
- 코딩 작업 가드레일은 Karpathy-inspired 4원칙(Think Before Coding / Simplicity First / Surgical Changes / Goal-Driven Execution)을 기본값으로 유지하고, 로컬 스킬(`skills/karpathy-guidelines/SKILL.md`)과 `AGENTS.md` 양쪽에 반영된 상태를 기준 운영으로 본다.
- 큰 작업/배경 작업/개발에이전트 작업이 끝나면, 검증·로그정리·상세설명보다 먼저 사용자에게 `끝남/핵심 변경/링크 또는 결과` 3요소로 1차 완료 보고를 즉시 보내는 것을 기본 상식으로 유지한다. 사용자가 다시 `다 됐어?`, `왜 보고 안 해?`라고 묻게 만들면 실패로 간주한다.
- 배경 코딩/쉘 작업은 가능하면 `scripts/completion_first_runner.py` 래퍼로 실행해, 종료 직후 OpenClaw system event로 1차 완료 보고가 자동 발화되게 운용한다.

## 운영 프로토콜(신규)
- `COGNITIVE_PROTOCOL.md`(Slim 1.1)를 운영 기준으로 채택: 실행 결과 파일 기록, 자동화 우선, "실행 먼저·결과만 보고" 유지.
- 보고는 근거(도구 결과/출처) 기반으로만 수행하고, 근거 없는 주장 보고를 금지.
- 진행/완료 상태 보고에는 증빙(PID, 파일경로, URL, 명령 출력 중 1개 이상)을 항상 포함하고, 증빙 없으면 "검증 전"으로 보고.
- 세션 시작 루틴에는 `runtime/insights/applied/behavior_rules.md`가 있으면 함께 읽어 자동 반영된 행동 규칙까지 현재 운영 기준으로 즉시 반영한다.

## 협업 구조(신규)
- `SONIC_DEV_TEAM.md`의 6역할(소닉/테일즈/에이미/너클즈/실버/섀도우) 분업 모델을 기준 템플릿으로 사용.
- 핸드오프는 목표·변경파일·테스트·리스크 4줄 포맷을 기본으로 유지.
- 크론 기반 하루 마감 점검에서는 대규모 untracked 작업물과 분리해, `memory/*`·`MEMORY.md` 같은 tracked 기록 변경만 독립 커밋/푸시하는 운영을 유지.
- 2026-03-15 재확인: 마감 점검 크론 지시가 오면 같은 턴에서 메모 갱신→tracked 변경 커밋→`origin/master` 푸시까지 완료하고, 사용자에게는 결과 요약만 단문 보고한다.
- 2026-03-16 재확인: 하루 마감 점검에서는 당일 핵심을 일일 메모에 남기고, 장기적으로 유지할 운영 원칙만 `MEMORY.md`에 최소 갱신한 뒤, 메모 관련 tracked 변경만 커밋·푸시하는 흐름을 유지한다.
- 2026-03-21 전략 설계 기준을 5개 레짐(`strong_bull / weak_bull / sideways / weak_bear / strong_bear`) 분리 운영으로 확정했고, 당분간 sideways는 기본 no-trade로 유지한다.
- 2026-03-21 기준 통합 적응형보다 `strong_bull` 단독 체크리스트형 접근의 검증 수치가 우세해, 구현 우선순위를 `strong_bull v2 → weak_bull v2 → weak_bear v2 → strong_bear v2`로 둔다.
- 2026-03-22 추가 검증으로 `strong_bull`은 BTC-first만 신뢰 가능한 방향이 더 강해졌고, ETH/SOL은 core deploy 대상에서 제외 유지한다.
- 2026-03-22 기준 `weak_bull`은 계속 비활성, `sideways`는 no-trade 유지, `weak_bear`는 ETH-first paper candidate 수준, `strong_bear`는 ETH/SOL continuation-short 참고 후보이되 샘플 부족으로 승격 보류 상태로 정리한다.
- 2026-03-22 기준 `strong_bull` 세부 보완에서는 `volume_confirmation_resume_only`가 BTC에서 베이스라인 대비 개선 신호를 보여, 향후 strong_bull 개선은 BTC 거래 재개형 볼륨 확인 로직 중심으로 이어간다.
- 2026-03-22 미장 아침 브리핑 자동화를 위해 `us-stock-briefing-bot` MVP CLI 골격을 작성했다(무료 공개 소스 기반 브리핑 생성 구조 확보).
- 2026-03-23 autoresearch 운영은 `workflow 문서 + 공용 실험 로그 템플릿 + 프로젝트별 experiments.log + 다음 실험 큐`를 한 세트로 남겨, 아이디어 수집보다 검증 이력과 다음 액션이 이어지는 구조를 기본값으로 삼는다.
- 2026-03-24 운영 방향은 단일 기능 추가보다 `인사이트 수집→판정→실행→검증`, 메모리/지식 인덱스, 에이전트 무결성 점검, 리포트 브리핑 봇까지 포함한 **운영 자동화 레이어 확장**을 우선 축으로 유지한다.
- 2026-03-25 ten-step paper strong_bear 보완에서는 `ETH+SOL`보다 **ETH-only overlay**가 더 나은 후보라는 재확인 결과가 나왔고, 당분간은 `활성 설정 미변경 + promote-to-next-paper-candidate` 원칙으로 유지한다.
- 2026-03-27 재확인: 하루 마감 점검 커밋은 대규모 untracked 작업물과 섞지 말고, `memory/*`·`MEMORY.md` 같은 기록성 변경만 분리해 `origin/master`까지 닫는 운영을 유지한다.
- 2026-03-28 재확인: 마감 시 `tracked 변경 없음 + 대규모 untracked 누적` 상태여도, 해당 턴에서 생성한 일일 메모와 장기 메모 최소 갱신만 별도 커밋·푸시로 닫고 untracked 정리는 분리 유지한다.
- 2026-03-29 기준, 마스터가 기억 보존을 요청한 `gitlab.com/Starmixcraft/tesla-fsd-can-mod`는 Tesla CAN 버스의 오토파일럿 관련 메시지를 수정/재전송해 FSD enable bit·속도 프로필·nag suppression 등을 제어하는 Adafruit Feather CAN 기반 펌웨어 저장소로 기억한다.
- 2026-03-30 재확인: 마스터는 TradingView 분석에서 웹보다 **Windows TradingView Desktop 앱을 직접 보고 판단하는 경로**를 우선 선호한다.
- 2026-03-30 재확인: Windows TradingView/Desktop CDP를 WSL/OpenClaw에서 붙일 때는 **앱의 remote-debugging 포트와 portproxy 진입 포트를 반드시 분리**한다(권장: 앱 9224, WSL 진입 프록시 9225).
- TradingView Pine 작업의 미완료 문맥으로 `EMA + WMA + Bollinger Bands + simplified POC` 조합의 **지표 + 전략 + README** 재구현/복구 작업이 남아 있으며, 이전에는 내용 생성까지 됐고 저장 단계에서 막힌 이력이 있다.
- 2026-04-01 확정: 외부 상위 repo/오픈소스는 설치·채굴 출처로만 활용하고, 실제 운영 반영은 로컬 문서/스크립트/스킬로 이식하는 clean-room/cherry-pick 방식을 기본값으로 유지한다.
- 2026-04-01 확정: 개발 하니스는 `runtime/assistant_structure` + `scripts/sonic_runtime_structure.py` 중심의 로컬 구조로 계속 키우고, 외부 repo 의존보다 로컬 운영 계층 강화에 우선순위를 둔다.
- 2026-04-02 확정: 장시간 백그라운드 작업은 `scripts/completion_first_runner.py`로 감싸 종료 직후 system event 1차 완료 보고를 우선 보내는 흐름을 기본 운영으로 유지한다.
- 2026-04-02 확정: 메모리/인사이트 운영의 기준 산출물은 `runtime/memory/memory.db`, `runtime/insights/*`, `runtime/knowledge_index.json`으로 보고, 저장뿐 아니라 검색·반영 루프가 실제 파일로 남는 구조를 유지한다.
- 2026-04-04 재확인: 마감 점검 시 작업트리에 다른 tracked 변경(예: runtime 상태파일 수정)이 함께 있어도, 자동 커밋 범위는 그 턴에서 갱신한 메모 기록(`memory/*`, `MEMORY.md`)으로 분리해 닫는 운영을 유지한다.
- 2026-04-05 결정: Karpathy의 `llm-wiki` 패턴을 워크스페이스에 도입한다. 전면 롤아웃보다 먼저 `knowledge/` 레이어를 만들어 트레이딩/전략/자동화 지식부터 raw/wiki/schema 3층 구조로 축적하고, 기존 `MEMORY.md`/`memory/*.md`와 병행 운용한다.
- 2026-04-06 확정: `korean-story-shorts-generator`는 clean-room MVP로 유지하며, 입력 경로를 `manual_story.yaml` + 공개 URL ingestion(본문 추출 → 검토용 manual_story 생성) 2축으로 운용한다. 자동 수집물은 업로드 완성본으로 간주하지 않고 항상 사람 검수 전제를 유지한다.
- 2026-04-07 확정: `korean-story-shorts-generator`의 공개 URL 자동 추출은 이미지 위주 게시물에서 파일명만 빨려 들어가는 실패가 발생할 수 있으므로, **실제 이미지/B-roll 없이 자동 산출물을 업로드 가능본으로 취급하지 않는다**. 쇼츠 품질 평가는 "상위권 실제 레퍼런스 재현 여부"를 기준으로 하고, 보고도 `업로드 가능/불가`를 과장 없이 이분화한다.
- 2026-04-08 기준, 마스터가 첨부한 `KDP Portal 작업 정리 (최종 통합본)`을 KDP Portal 관련 대화의 기준 문서로 기억한다. 프로젝트 축은 라이나생명보험 KDP Portal(DB INC 프리랜서), 백엔드는 Spring Boot/JPA-Hibernate/QueryDSL/SQL Server, 패키지 축은 `com.kdp.portal`(role/approval/batch) + `com.kdp.cmm`(board/file/common)이며, 실수 포인트로는 `@IdClass` vs `@EmbeddedId`, QueryDSL JPQL 필드명, `yyyyMMdd` 날짜 포맷, `roleAprvCd` String 타입, Eclipse Full Clean/Rebuild 필요가 있다.
- 2026-04-10 마감 점검 재확인: 크론 원문에 `tracked 변경이 있으면 commit 후 origin/master push`가 명시되면, 메모 파일만 분리하는 기존 관성보다 **해당 턴의 tracked 변경 전체 커밋·푸시 지시를 우선 적용**한다.
- 2026-04-12 재확인: 하루 마감 점검에서는 당일 `memory/YYYY-MM-DD.md` 생성과 `MEMORY.md` 최소 갱신을 먼저 완료하고, Git 정리는 대규모 untracked 작업물과 섞지 않도록 메모 관련 변경만 분리 커밋·푸시하는 운영을 유지한다.
- 2026-04-13 확정: Q00/ouroboros는 워크스페이스 로컬 clone(`external/ouroboros`) + 전용 venv(`.venvs/ouroboros`) + 로컬 MCP 엔트리로 붙인 구성을 기준선으로 유지한다.
- 2026-04-14 확정: research-skill-graph/llm wiki 계열 외부 인사이트는 "새 시스템으로 교체"보다 현재 로컬 `knowledge/`·`MEMORY`·인사이트 파이프라인을 강화하는 참고 모델로 흡수하는 방향을 기본값으로 둔다.
- 2026-04-14 확정: Obsidian은 지식 파일을 보는/편집하는 프론트엔드, llm wiki는 LLM이 읽고 갱신하는 지식 구조/운영 방식으로 구분하며, 상호 대체제가 아니라 병행 가능한 계층으로 기억한다.
- 2026-04-15 기준 `shared-food-map/shared-food-map`는 카카오맵에서 구글맵 전환 작업이 진행된 개인 테스트용 공유지도 프로젝트로 기억한다. 구글맵 적용 시 `client/.env`의 `VITE_GOOGLE_MAPS_API_KEY` 환경변수가 필요하고, 소액 가능성은 낮아도 billing 연결이 필요한 pay-as-you-go 구조라는 점을 함께 기억한다.
- 2026-04-16 확정: Bybit 계열 외부 bot 아이디어는 원본 직결 도입보다 `trading-automation`에 **clean-room overlay**로만 흡수하고, 기본 운용은 `shadow_only: true` / `overlay.apply_to_paper: false`를 유지한다. quick replay 개선 신호가 있어도 표본 부족·price-only 근사 한계가 남아 있어, 즉시 paper 승격은 하지 않는다.
- 2026-04-17 확정: BTC/ETH 비교 재검토 끝에, 마스터는 **실전형 차트 전략을 BTC 전용으로만 운용**하는 방향을 선택했다. 기준 구조는 `4H 방향 → 4H 위치 → 15m 레인지/스윕 → 5m MSS → OTE+FVG → 첫 리테스트 진입`의 보수적 체크리스트형 흐름으로 기억한다.
- 2026-04-19 확인: 현재 메인 LLM 연결은 `openai-codex` 1개이며, 향후 NVIDIA NIM 같은 추가 provider를 붙이더라도 소닉이 "둘"이 되는 개념이 아니라 **선택 가능한 추가 provider/모델 경로가 늘어나는 것**으로 이해한다.
- 2026-04-20 재확인: 증권사 리포트 자동화의 장기 기준은 예전 08:50 장전 요약이 아니라, **국장+미장 후보를 보기 좋게 정리하고 종목별 핵심 호재 1줄까지 붙이는 최신 포맷**이며, 실전형 발송 시각은 **08:40 KST + 21:30 KST** 2회다.
- 2026-04-22 확정: `shared-food-map` 검색 UX는 **검색 결과 클릭 → 지도 먼저 이동/프리뷰 표시 → 사용자가 저장 버튼을 눌렀을 때만 저장 모달 오픈** 흐름을 기준선으로 기억한다. 자동으로 저장 모달이 튀는 UX로 되돌리지 않는다.
- 2026-04-24 확정: X 추천 계정 검토에서는 전체 편입 대신 **선별 추가**를 기본 원칙으로 유지하고, `@itsCblast`, `@jordanfogel`는 종목발굴용 후보 소스가 아니라 **GEX/옵션 플로우/시장구조 보조 소스**로만 운영한다.
- 2026-04-30 확정: X 후보 계정/종목 검토는 프로필 전체를 뭉뚱그려 추론하지 말고, **사용자가 지목한 정확한 원글 링크를 먼저 열어 본문 기준으로 종목을 추출·검증**하는 절차를 기본값으로 유지한다.
- 2026-05-01 확정: X 후보 추적 리포트는 `내부 후보와 겹치는 종목`만 반복 상단 노출하지 말고, **신규 X후보 단독 종목 섹션을 분리 유지**해 새 종목이 보고에서 사라지지 않게 운용한다.
- 2026-05-01 확정: X 후보 수집에서는 `프로필 전체 긁기 노이즈 제거`를 우선하고, 새 종목 보고는 `겹침 후보`와 `X후보 단독`을 분리해 가시성을 보장하는 구조를 기본값으로 유지한다.
- 2026-05-02 확정: X 구독글/브라우저 수집은 `브라우저에서 한 번 읽힌 경험`과 `정기 자동화 경로가 현재 살아 있는 상태`를 분리해서 판단한다. 운영 점검의 1차 기준은 OpenClaw가 붙는 **CDP 9222 연결 생존 여부**로 둔다.
- 2026-05-03 확정: X 구독글 자동 루프는 **매 시간 Chrome을 새로 띄우는 구조가 아니라, 기존 전용 Chrome/CDP 세션이 살아 있으면 재사용하고 죽었을 때만 재기동**하는 동작을 기준선으로 기억한다. 상태 확인 시에는 `재기동 여부`보다 `수집 완료 후 60분 대기 진입` 로그를 우선 본다.
- 2026-05-04 확정: Hermes 쪽 한투모의 주식 보고는 **전략 managed 종목 기준**과 **계좌 전체 참고 보유분**을 반드시 분리해 설명한다. 전략 성과 판단과 보유 현황 요약은 managed 기준을 우선하며, 과거 `max_holdings=3` 같은 슬롯 제약 이력은 현재 상태처럼 섞어 말하지 않는다.
- 2026-05-06 확정: X 구독글 자동수집은 **기존 전용 Chrome/CDP 세션의 기존 탭을 우선 재사용**하고, 탭이 없을 때만 새 탭을 여는 동작을 기준선으로 유지한다. 오토루프가 돌 때마다 계정 URL 탭을 중복 생성하는 회귀는 버그로 간주한다.
- 2026-05-06 확정: X 구독글 운영에는 **매시간 수집 성공 여부 + 계정별 발언 요약**을 함께 점검/보고하는 시간별 요약 크론이 연결된 상태를 기준선으로 기억한다.
- 2026-05-08 확정: X 후보 성과 추적 보고는 **매일 08:45 KST 짧은 브리프 + 매주 금요일 16:00 KST 상세 리뷰** 스케줄을 기준선으로 유지한다.
- 2026-05-08 확정: 종목 출처 설명 시 `우리 자체 발굴`과 `X 후보/외부 후보`를 섞어 추정하지 않고, **기록에 남은 최초 포착 출처 기준으로 분리 확인 후 설명**하는 원칙을 유지한다.
- 2026-05-09 확정: 외부 스킬/하니스 저장소(예: `agent-skills`)는 전면 설치하지 않고, **참고용 clone + 필요한 규칙만 cherry-pick 이식**하는 방식으로 유지한다.

<!-- HERMES_BRIDGE_SYNC_START -->
## Hermes 브리지 최신 동기화(휘발성)
- 메인 진입: Hermes 텔레그램 DM
- 용도: 마스터가 Hermes에만 말해도 OpenClaw가 최신 맥락을 이어받기 위한 최근 대화 요약
- 마지막 갱신: 2026-05-11T22:34:57+09:00

### 최근 턴 1
- 시각(KST): 2026-05-11T22:09:06+09:00
- Hermes 세션: 20260511_213519_4417a6
- 사용자: 너 방금 엑스글 어디까지 읽엇는지 나에게 이야기해봐
- Hermes 응답: 마스터, 제가 읽은 건 **해당 X 원트윗 본문 전체로 보이는 7단계 목록까지**입니다. 정확히 확인한 범위는 이겁니다. - 작성자: **백도🍑 (@WhitePeach)** - 글 시작: **“트레이딩 입문하시려면 꼭 다음의 과정을 거치세요”** - 읽은 내용: 1. 매매에 적용할 기본이론 및 패턴 학습 2. 리스크관리 시스템 구축 3. 소액 실전 연습 4. 기계적 손절 훈련 5. 베팅금액 단계적 확대 6. 수익 누적 및 전략 고도화 7. 트레이딩 마인드셋 정립 각 항목의 하위 bullet도 읽…
### 최근 턴 2
- 시각(KST): 2026-05-11T22:11:12+09:00
- Hermes 세션: 20260511_220845_f0d49c
- 사용자: 항상 원글밑에 답글도 확인해서 글이 이어진건지 아닌지 확인하면 좋겠는데
- Hermes 응답: 맞습니다, 마스터. 이건 제가 기준을 올려야 합니다. 앞으로 X 글 분석할 때는 **원트윗 본문만 읽고 끝내지 않고**, 항상 아래를 먼저 확인하겠습니다. - 원글 밑에 **작성자 본인의 답글/셀프리플라이**가 있는지 - 글이 **단독 글인지, 이어지는 스레드인지** - 번호형 글이면 **끝 번호까지 확인됐는지** - 답글이 막혀 있으면 **“원트윗만 확인, 이어진 답글은 미검증”**이라고 명시 방금 이 운영 기준은 메모리에 반영했고, X 스레드 복구 스킬에도 패치했습니다. 이번 WhitePea…
### 최근 턴 3
- 시각(KST): 2026-05-11T22:14:01+09:00
- Hermes 세션: 20260511_220845_f0d49c
- 사용자: 그 1번부터 7번까지 어떤식으로 실행해야 좋을까
- Hermes 응답: TeamDark 사용 안 함 — 이건 분석보다 **실행 루틴 설계**라 단일 흐름으로 정리하겠습니다. 마스터, WhitePeach 글의 1~7번은 이렇게 실행하면 좋습니다. 핵심은 **공부 → 규칙 → 소액실전 → 손절훈련 → 증액 → 전략개선 → 멘탈관리** 순서입니다. --- ## 전체 실행 방식 한 줄 요약 ```text 전략 1개 선택 → 이론을 진입/손절 기준으로 정리 → 리스크 규칙 고정 → 극소액/페이퍼 50회 거래 → 손절 준수율 확인 → 기준 통과 시만 금액 확대 → 반복 가능한…
### 최근 턴 4
- 시각(KST): 2026-05-11T22:23:20+09:00
- Hermes 세션: 20260511_220845_f0d49c
- 사용자: https://x.com/RealJGBanks/status/2053521206532518224?s=20 이거 영어영상이라 알수가없는데 내가 공부할수있는 방법이있을까
- Hermes 응답: TeamDark 사용 안 함 — 이건 영어 영상을 **마스터가 공부 가능한 자료로 바꾸는 작업**입니다. 마스터, 가능합니다. 이 영상은 그냥 “영어 들어가며 보기”로 공부하면 효율이 낮고, 아래 방식으로 해야 합니다. 제가 확인한 원글 기준: - 작성자: Justin Banks / @RealJGBanks - 내용: **$SPY 1시간 트레이딩 클래스** - 영상 길이: 약 **57분 11초** - 주제: - 시장 읽기 - 좋은 셋업 고르기 - 리스크 관리 - 포지션 사이징 - 그 외 SPY 매…
### 최근 턴 5
- 시각(KST): 2026-05-11T22:34:57+09:00
- Hermes 세션: 20260511_220845_f0d49c
- 사용자: [The user sent a document: '2025년판_우체국_추천_맛집가이드부산지방우정청.pdf'. The file is saved at: /home/hskim/.hermes/cache/documents/doc_4723c2a09ac2_2025년판_우체국_추천_맛집가이드부산지방우정청.pdf. Ask the user what they'd like you to do with it.] 맛집저장해줘 전부다
- Hermes 응답: 완료했습니다, 마스터. PDF에서 **맛집 245개 전부 추출해서 저장**했습니다. PDF 표지에 적힌 총 수록 수가 245개였고, 추출 결과도 **245개로 일치** 확인했습니다. ## 저장 결과 - 전체 맛집 수: **245개** - 추천 우체국 수: **37개** - 권역별: - 부산권: **95개** - 울산권: **19개** - 경남권: **131개** ## 저장 파일 ### Obsidian/Markdown용 전체 목록 ```text /home/hskim/.openclaw/workspa…

<!-- HERMES_BRIDGE_SYNC_END -->

